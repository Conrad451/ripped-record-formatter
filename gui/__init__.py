"""PySide6 desktop GUI for Ripped Record Formatter.

This is the new primary interface. It is a thin UI layer over the ``core``
package: it gathers input, runs conversions on a background thread, and renders
progress. All audio/metadata logic stays in ``core``.
"""
